[workspace]
resolver = "2"
members = [
    "property-registry",
    "mortgage-pool",
    "build-escrow",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
strip = true
lto = true
panic = "abort"
codegen-units = 1
