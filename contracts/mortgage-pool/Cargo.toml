[package]
name = "mortgage-pool"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "26.0.1"
# We can use a contractimport or define the interface of PropertyRegistry to call it.
# We will define a simple interface for PropertyRegistry in the code.
